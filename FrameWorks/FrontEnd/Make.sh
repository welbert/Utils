#!/bin/bash
#Esse make deve ser copiado para os projetos que possuem o build por grunt
#É necessário ter o pacote npm e e ter o grunt -cli instalado
#sudo apt-get install npm -y
#sudo ln -s /usr/bin/nodejs /usr/bin/node
#sudo npm install -g grunt-cli 

sudo npm install
grunt